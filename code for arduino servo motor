#include <Servo.h>

Servo servo;

void setup() {
  servo.attach(6); // Control pin connected to D6
}

void loop() {
  servo.write(0);   // Move to 0°
  delay(500);
  servo.write(90);  // Move to 90°
  delay(500);
  servo.write(180); // Move to 180°
  delay(500);
}
